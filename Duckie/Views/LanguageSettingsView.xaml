<UserControl x:Class="Duckie.Views.LanguageSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:loc="clr-namespace:Duckie.Utils.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="{loc:Localize Dialog_LanguageSettings}" FontSize="16" FontWeight="Bold"
                   Margin="0,0,0,20" Foreground="#333"/>

        <!-- 语言选择 -->
        <TextBlock Grid.Row="1" Text="{loc:Localize Label_SelectLanguage}" FontSize="13"
                   Margin="0,0,0,8" Foreground="#333"/>
        
        <ComboBox x:Name="LanguageComboBox" Grid.Row="2" Height="32" FontSize="13"
                  SelectionChanged="LanguageComboBox_SelectionChanged">
            <ComboBoxItem Content="English" Tag="en-US"/>
            <ComboBoxItem Content="简体中文" Tag="zh-CN"/>
        </ComboBox>

        <!-- 说明文字 -->
        <TextBlock Grid.Row="3" TextWrapping="Wrap" FontSize="11" Foreground="#666"
                   Margin="0,15,0,0" VerticalAlignment="Top">
            <Run Text="{loc:Localize Label_LanguageNote}"/>
            <LineBreak/>
            <Run Text="{loc:Localize Label_LanguageRestart}"/>
        </TextBlock>
    </Grid>
</UserControl>
