<UserControl x:Class="Duckie.Views.Controls.NavigationMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="200">
    
    <Button x:Name="MenuButton" 
            HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
            Padding="0" Margin="0" Height="48" Cursor="Hand"
            Click="MenuButton_Click">
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Selection indicator -->
                                <Rectangle x:Name="SelectionIndicator" Grid.Column="0" 
                                          Fill="#007ACC" Width="3" Visibility="Collapsed"/>
                                <!-- Content area -->
                                <Grid Grid.Column="1" Margin="12,8,16,8">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E9ECEF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#DEE2E6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Button.Style>
        
        <StackPanel Orientation="Horizontal">
            <Canvas Width="20" Height="20" Margin="0,0,12,0">
                <Path x:Name="IconPath" Fill="#495057" StrokeThickness="0"/>
            </Canvas>
            <TextBlock x:Name="TextBlock" VerticalAlignment="Center" Foreground="#495057"/>
        </StackPanel>
    </Button>
</UserControl>
