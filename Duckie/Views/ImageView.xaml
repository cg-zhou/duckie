<UserControl x:Class="Duckie.Views.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Duckie.Views"
             xmlns:common="clr-namespace:Duckie.Views.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             AllowDrop="True"
             Drop="ImageView_Drop"
             DragEnter="ImageView_DragEnter"
             DragLeave="ImageView_DragLeave">
    <!-- 样式已移动到全局主题文件中，减少内存占用 -->

    <DockPanel>
        <!-- Modern Toolbar -->
        <Border DockPanel.Dock="Top" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- File Operations -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Open" Click="ButtonOpen_Click"
                            Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"/>
                    <Button Content="Export ICO" Click="ButtonSave_Click"
                            Style="{StaticResource ModernButtonStyle}"/>
                </StackPanel>

                <!-- Edit Tools -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Transform Group -->
                    <Border Background="White" CornerRadius="6" Padding="8,4" Margin="0,0,15,0"
                            BorderBrush="#DEE2E6" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Transform" FontSize="11" Foreground="#6C757D"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <!-- Rotate 270° Counter-clockwise (Left) -->
                            <common:ButtonIcon Click="ButtonRotate270_Click" ToolTip="Rotate 90° Counter-clockwise"
                                               Margin="0,0,4,0" IconType="ArrowCounterClockwise"/>

                            <!-- Rotate 90° Clockwise (Right) -->
                            <common:ButtonIcon Click="ButtonRotate90_Click" ToolTip="Rotate 90° Clockwise"
                                               Margin="0,0,8,0" IconType="ArrowClockwise"/>

                            <!-- Flip Horizontal -->
                            <common:ButtonIcon Click="ButtonFlipH_Click" ToolTip="Flip Horizontal"
                                               Margin="0,0,4,0" IconType="FlipHorizontalFill"/>

                            <!-- Flip Vertical -->
                            <common:ButtonIcon Click="ButtonFlipV_Click" ToolTip="Flip Vertical"
                                               IconType="FlipVerticalFill"/>
                        </StackPanel>
                    </Border>

                    <!-- Zoom Group -->
                    <Border Background="White" CornerRadius="6" Padding="8,4"
                            BorderBrush="#DEE2E6" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Zoom" FontSize="11" Foreground="#6C757D"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <common:ButtonIcon Click="ButtonZoomOut_Click" ToolTip="Zoom Out"
                                               Margin="0,0,4,0" IconType="ZoomIn"/>

                            <Slider x:Name="ZoomSlider" Width="80" Minimum="0.1" Maximum="3.0" Value="1.0"
                                    VerticalAlignment="Center" Margin="0,0,4,0" ValueChanged="ZoomSlider_ValueChanged" />

                            <common:ButtonIcon Click="ButtonZoomIn_Click" ToolTip="Zoom In"
                                               Margin="0,0,8,0" IconType="ZoomOut"/>

                            <common:ButtonIcon Click="ButtonZoomFit_Click" ToolTip="Fit to Window" IconType="ExpandSolid"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
        <StatusBar DockPanel.Dock="Bottom" Height="25" Padding="6 0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="ImageInfoText" Text="" />
            </StatusBarItem>
        </StatusBar>
        <DockPanel>
            <Grid>
                <!-- 优化的棋盘背景 - 保持视觉效果，减少内存占用 -->
                <Rectangle>
                    <Rectangle.Fill>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <!-- 使用更简单的实现，但保持棋盘效果 -->
                                <DrawingGroup>
                                    <GeometryDrawing Brush="#FFEEEEEE">
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,20,20" />
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                    <GeometryDrawing Brush="#FFDDDDDD">
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <RectangleGeometry Rect="0,0,10,10" />
                                                <RectangleGeometry Rect="10,10,10,10" />
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <common:ScrollViewerEx>
                    <Image x:Name="image" />
                </common:ScrollViewerEx>
            </Grid>
        </DockPanel>
    </DockPanel>
</UserControl>
